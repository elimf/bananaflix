apiVersion: v1
kind: Service
metadata:
  name: {{ include "bananaflix.fullname" . }}
  labels:
    {{- include "bananaflix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bananaflix.selectorLabels" . | nindent 4 }}
