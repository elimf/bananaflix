apiVersion: apps/v1
kind: Deployment
metadata:
  name: mqtt-service
  labels:
    app: mqtt-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: mqtt-service
  template:
    metadata:
      labels:
        app: mqtt-service
    spec:
      containers:
      - name: mqtt-service
        image: safouuuuu/mqtt-service:main
        ports:
        - containerPort: {{ .Values.service.port }}
        resources:
          limits:
            cpu: {{ .Values.resources.limits.cpu }}
            memory: {{ .Values.resources.limits.memory }}
          requests:
            cpu: {{ .Values.resources.requests.cpu }}
            memory: {{ .Values.resources.requests.memory }}