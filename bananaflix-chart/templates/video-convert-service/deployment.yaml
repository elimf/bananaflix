apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-convert-service
  labels:
    app: video-convert-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: video-convert-service
  template:
    metadata:
      labels:
        app: video-convert-service
    spec:
      containers:
      - name: video-convert-service
        image: safouuuuu/video-convert-service:main
        ports:
        - containerPort: {{ .Values.service.targetPort }}
        resources:
          limits:
            cpu: {{ .Values.resources.limits.cpu }}
            memory: {{ .Values.resources.limits.memory }}
          requests:
            cpu: {{ .Values.resources.requests.cpu }}
            memory: {{ .Values.resources.requests.memory }}